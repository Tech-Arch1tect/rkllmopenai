services:
  app:
    restart: always
    image: rkllm-openapi:latest
    build: .
    ports:
      - "8080:8080"
    environment:
      - STORAGE_PATH=/rkllm
    volumes:
      - ../storage:/rkllm
    privileged: true
